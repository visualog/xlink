{
  "name": "xlink",
  "version": "0.1.0",
  "private": true,
  "description": "Lightweight local coordinator for agent handoffs",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test tests/*.test.js",
    "cli": "node src/cli.js",
    "migrate:sqlite": "node src/cli.js import-json --source data/handoffs.json --target data/handoffs.sqlite --target-backend sqlite --skip-existing",
    "backup:json": "node src/cli.js export-json --source data/handoffs.sqlite --source-backend sqlite --target data/handoffs.backup.json",
    "dashboard:build": "node src/build-dashboard.js"
  }
}
